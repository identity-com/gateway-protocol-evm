/**
 * Goals of this test:
 * 1) Connect to an RPC node for transactions
 * 2) Hardcode private keys of dev wallets (do not save anywhere)
 * 3) Create a network with a gatekeeper
 *          - Require gatkeepers stake
 *          - Add gatekeeper stake
 * 4) Issue a token
 * 5) Verify an issued token
 */